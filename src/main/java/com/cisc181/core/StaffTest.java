package com.cisc181.core;

import static org.junit.Assert.*;
import org.junit.Before;
import org.junit.After;
import org.junit.Test;

public class StaffTest {
	public String Staff;
@Before
public void setUp()throws Exception{
	List StaffSaleries = new ArrayList();
	StaffSaleries.add(123456);
	StaffSaleries.add(234567);
	StaffSaleries.add(345678);
	StaffSaleries.add(456789);
	StaffSaleries.add(567890);
	
	Average = ArrayList / 5
}
@After
public void tearDown()throw Exception{
	StaffSaleries = null;
}
@Test
public final void testPhone_Number()throws PersonException{
	System.out.println(Average.getAverage());
	assertTrue((double)Average.getAverage()==(double)Average);
}

}
